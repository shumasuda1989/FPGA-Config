#ifndef __SPI_VERIFY__
#define __SPI_VERIFY__

#include <iostream>
#include <fstream>
#include <cstdlib>

void spi_verify(const char *ip_addr, unsigned int port, unsigned int file_size, const char *conv_file_name);

#endif
